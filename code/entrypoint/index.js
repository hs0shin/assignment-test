// import module scripts you want; 
// e.g. import {something} from './someModule';

function initialize () {
    // 처음 스크립트 로딩이 다 끝난 다음 일어날 일들을 처리한다. 페이지 초기화에 시킬 일들을 작성한다
   }
   
   export default () => window.onload = initialize;